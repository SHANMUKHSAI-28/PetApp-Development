import React from "react";
import { Typography } from "@mui/material";

const Home = () => {
  return (
    <div>
      <Typography variant="h4">Home Page</Typography>
      {/* Add product listings, etc. here */}
    </div>
  );
};

export default Home;